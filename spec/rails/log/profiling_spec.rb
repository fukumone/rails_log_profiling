require "spec_helper"

describe Rails::Log::Profiling do
  it "has a version number" do
    expect(Rails::Log::Profiling::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
